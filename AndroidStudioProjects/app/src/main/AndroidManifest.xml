<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.aakash.testing">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission
        android:name="android.permission.MEDIA_CONTENT_CONTROL"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.DarkActionBar">
        <activity
            android:name=".MainActivity"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainPageActivity" />
        <activity android:name=".AuthActivity" />
        <activity android:name=".UserNameSpeakActivity" />
        <activity android:name=".LoginPhone" />
        <activity android:name=".ProfileUploadActivity" />
        <activity android:name=".SlidingInfoActivity" />
        <activity android:name=".MagicButtonActivity" />
        <activity android:name=".ChatbotActivity" />
        <activity android:name=".FriendsConnectActivity" />
        <activity android:name=".YoutubeConnectActivity" />
        <activity android:name=".CheckPhotosActivity" />
        <activity android:name=".NearByActivity" />
        <activity android:name=".CollapsingInformation" />
        <activity android:name=".DetailsActivity" />
        <activity android:name=".ChatBotSF" />
    </application>

</manifest>